# /context — Load Relevant Knowledge

Load promoted knowledge files by topic into the conversation context.

## Steps

Invoke the aria-knowledge **`context`** skill with the tag(s) provided. Queries the tag index and presents matching files for selective loading.

Examples: `/context stripe`, `/context api pagination`, `/context cs AND auth`.
