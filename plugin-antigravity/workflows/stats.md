# /stats — Knowledge Base Health

Show knowledge base health metrics: file counts, backlog depth, audit status, codemap freshness, tag coverage.

## Steps

Invoke the aria-knowledge **`stats`** skill — it walks the knowledge folder and emits a dashboard view of the knowledge base's current state.
